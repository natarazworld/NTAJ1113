CREATE TABLE "SYSTEM"."FILE_UPLOAD" 
   (	"EMPNO" NUMBER(10,0) NOT NULL ENABLE, 
	"ENAME" VARCHAR2(20 BYTE), 
	"EPHOTO" VARCHAR2(40 BYTE), 
	"ERESUME" VARCHAR2(40 BYTE), 
	 CONSTRAINT "FILE_UPLOAD_PK" PRIMARY KEY ("EMPNO"));
	 =========================================================
	 CREATE SEQUENCE  "SYSTEM"."ENO_SEQ"  MINVALUE 1 MAXVALUE 10000 INCREMENT BY 1 START WITH 1 ;